# HEARTBEAT.md Template

```markdown
# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Add tasks below when you want the agent to check something periodically.
# Examples:
# - Run `pnpm run test` periodically to ensure system stability.
# - Monitor local worker health in `apps/worker`
# - Check CLI TUI execution readiness `node apps/cli/index.js --help`
```
