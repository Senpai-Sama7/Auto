[project]
name = "omega-docker-mcp"
version = "1.0.0"
description = "Docker container management MCP server"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "structlog>=24.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
